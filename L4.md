# PER-003 L4 스위치 요구규격 브로셔 근거 확인

대상 문서:
- 제안요청서: `제안요청서.md`
- 요구사항 매트릭스: `requirements_evidence_matrix.md`
- 브로셔: `[국문] PAS-K 브로셔(2026년).pdf`

확인 대상 요구사항:
- 요구사항 번호: `PER-003`
- 요구사항 명칭: 도입장비 요구규격 3
- 정의: L4 스위치에 대한 요구규격

브로셔상 권장 최소 대응 모델:
- `PAS-K5200`
- 사유: `Throughput 16Gbps`, `Concurrent Sessions 18,000,000`, `10GbE SFP+ 16, 8(optional)`, `1GbE Copper 8(optional)`, `dual / hot-swappable power`로 PER-003의 정량 요구를 충족하는 최소 물리 어플라이언스 모델로 확인됨.

주의:
- 브로셔는 제품 기능/사양 근거자료로 활용 가능하나, `제조사 정품 HW/SW 제공`, `Transceiver 포함`, `타 제조사 장비 상호연동 운용 가능` 등은 브로셔 단독으로는 완전 입증이 어렵습니다. 해당 항목은 제조사 확인서, 정품공급 및 규격증명서, 물품공급 및 기술지원 확약서, 구성 산출물로 보완 증빙이 필요합니다.

## 1. 요구사항별 위치 및 근거

| PER-003 요구항목 | 브로셔 위치 | 브로셔 근거 내용 | 판정 | 비고 |
| --- | --- | --- | --- | --- |
| 처리 성능 - Throughput 10Gbps 이상 | p.5, `제품사양`, `PAS K5200 K4300 K5400 K5600 K8620R` 표 | `PAS K5200`의 `Throughput (L4/L7)`가 `16 Gbps / 12 Gbps`로 기재됨 | 충족 | L4 Throughput 기준 16Gbps로 요구 10Gbps 이상 |
| 처리 성능 - 동시세션 1,800만 세션 이상 | p.5, 같은 제품사양 표 | `PAS K5200`의 `Concurrent Sessions`가 `18,000,000`으로 기재됨 | 충족 | 요구값 1,800만 세션과 동일 |
| 인터페이스 - 10G SFP+ 8포트 이상 | p.5, 같은 제품사양 표 | `PAS K5200`의 `10 GbE Fiber (SFP+)`가 `16, 8(optional)`로 기재됨 | 충족 | 구성 선택 시 8포트 이상 충족 가능. 제안 구성서에 실제 포트 수 명시 필요 |
| 인터페이스 - 1G SFP 또는 1G Copper 8포트 이상 | p.5, 같은 제품사양 표 | `PAS K5200`의 `1 GbE Copper`가 `8(optional)`로 기재됨 | 조건부 충족 | 옵션 구성으로 8포트 확보 필요. 견적/구성명세서에 옵션 반영 필요 |
| 기능 - FWLB/SLB 기능 지원 | p.4, `PAS-K·KV 주요 기능`, `Layer 4` | `server load balancing`, `firewall load balancing` 기재 | 충족 | `SLB=server load balancing`, `FWLB=firewall load balancing`로 대응 |
| 기능 - 세션 관리 및 헬스체크 기능 지원 | p.2, `고가용성`; p.4, `Health Check`; p.4, `High Availability`; p.4, `Layer 7` | p.2에 `세션 동기화 및 설정 동기화` 기재. p.4에 `Health Check link, ARP, ICMP, TCP, UDP, HTTP, script...` 및 `stateful active-standby failover` 기재 | 충족 | 헬스체크 방식과 세션 동기화/상태 기반 장애조치 근거 확인 |
| 기능 - 정책 기반 트래픽 제어 | p.3, `트래픽 제어 및 안정적인 서비스 제공`; p.4, `Layer 7`; p.4, `PREScript` | p.3에 `트래픽 폭주 제어 및 연결보장` 기재. p.4에 `URL-based load balancing`, `cookie-based load balancing`, `traffic surge protection`, `PREScript(rule & event script)` 기재 | 충족 | 정책/룰 기반 트래픽 제어 기능 근거로 활용 가능 |
| 기능 - 전원 이중화 지원 | p.5, 제품사양 표 | `Power Supply dual / hot-swappable power` 기재 | 충족 | 이중 전원 및 Hot-swap 지원 |
| 기능 - L4 스위치 간 이중화 구성 지원 | p.2, `고가용성`; p.4, `High Availability` | p.2에 `HA 및 VRRP 방식 등 다양한 Failover 기능과 세션 동기화 및 설정 동기화 등 제공` 기재. p.4에 `HA, eVRRP, MVRRP, stateful active-standby failover, active-active failover` 기재 | 충족 | 이중화 방식은 사업집행부대/대상부대 협의 조건에 맞춰 설계서로 보완 |
| 기능 - 타 제조사의 장비 상호연동 운용 가능 | p.4, `연동 보안 솔루션`; p.4, `Management` | p.4에 `연동 보안 솔루션`으로 NGFW, IPS/IDS, WAF, DLP, Anti-malware 흐름 표시. `SNMP`, `syslog`, `REST API`, `RADIUS`, `TACACS+` 등 관리 연동 기능 기재 | 부분 충족 | 브로셔상 보안장비/관리 연동 기능은 확인되나 “타 제조사 장비 상호운용 가능”은 제조사 확인서 또는 연동시험 결과로 보완 권장 |
| 기능 - 보안기능확인서 보유장비 | p.2, 하단 `보안기능확인서` 표기 | `보안기능확인서` 문구가 PAS-K/PAS-KV 소개 영역에 표기됨 | 부분 충족 | 실제 제안 모델과 버전의 보안기능확인서 원본/사본 및 SW 해시값 대조표 필요 |
| 기능 - 제조사 정품 HW 및 SW 제공 | p.2, `PAS-K`; p.5, 제품사양 | p.2에 `자체 개발 전용 ADC 어플라이언스` 기재. p.5에 PAS-K 물리 장비 제품사양 기재 | 부분 충족 | 브로셔는 제조사 제품임을 보여주는 보조 근거. 정품 공급 및 규격 증명서, 라이선스 증서, Transceiver 포함 구성명세서 필요 |

## 2. PAS-K5200 기준 PER-003 정량 요구 대응

| 구분 | PER-003 요구 | PAS-K5200 브로셔 수치 | 판정 |
| --- | --- | --- | --- |
| Throughput | 10Gbps 이상 | L4 16Gbps | 충족 |
| 동시세션 | 1,800만 세션 이상 | 18,000,000 | 충족 |
| 10G SFP+ | 8포트 이상 | 16 또는 8(optional) | 충족 |
| 1G SFP 또는 1G Copper | 8포트 이상 | 1G Copper 8(optional) | 조건부 충족 |
| 전원 | 이중화 지원 | dual / hot-swappable power | 충족 |

## 3. 제안서에 넣을 수 있는 근거 문안

`PER-003 L4 스위치 요구규격은 파이오링크 PAS-K 브로셔(2026년) 기준 PAS-K5200 모델의 제품사양(p.5)에서 L4 Throughput 16Gbps, Concurrent Sessions 18,000,000, 10GbE Fiber(SFP+) 16 또는 8(optional), 1GbE Copper 8(optional), Power Supply dual/hot-swappable power로 확인되어 정량 요구사항을 충족한다. 또한 주요 기능(p.4)에서 Layer 4의 server load balancing 및 firewall load balancing, Health Check, HA/eVRRP/MVRRP, stateful active-standby failover 및 active-active failover, Layer 7의 traffic surge protection 및 PREScript(rule & event script) 등이 확인되어 SLB/FWLB, 헬스체크, 이중화 및 정책 기반 트래픽 제어 요구에 대응 가능하다. 다만 1G Copper 8포트는 optional 구성으로 명시되어 있으므로 실제 제안 구성명세서 및 견적서에 해당 옵션을 반영하고, 보안기능확인서 및 정품 HW/SW 제공 항목은 제조사 발급 증명서와 보안기능확인서 원본/사본으로 별도 증빙한다.`

## 4. 보완 증빙 필요 목록

| 보완 대상 | 필요한 증빙 |
| --- | --- |
| 1G Copper 8포트 옵션 구성 | 모델별 구성명세서, 견적서, BOM |
| Transceiver 모듈 포함 | Transceiver 종류/수량이 포함된 납품 구성표, 제조사 정품 확인서 |
| 제조사 정품 HW/SW 제공 | 정품공급 및 규격 증명서, SW 라이선스 증서 |
| 보안기능확인서 보유 | 제안 모델/버전의 보안기능확인서, SW 해시값 대조표 |
| 타 제조사 장비 상호연동 | 제조사 상호연동 확인서, 구축/연동 시험계획서 및 결과서 |
